<%= form_for([@item, @item.comments.build]) do |f| %>
<%= f.text_area :content, placeholder: "Comment", class: "textarea" %>
<%= f.hidden_field :audience_id, value: current_user.id %>
<div class= "spacer_between_submit"></div>
<%= f.button :submit,:class => 'btn btn-warning' %>
<% end %>
