<%#= form_for @comment do |f| %>
<%#= f.text_area :content %>
<%#= f.hidden_field :audience_id, :value=>@audience_id %>
<%# I think this is wrong %>

<%= form_for([@item, @item.comments.build]) do |f| %>

<%= f.text_area :content %>
<%= f.hidden_field :audience_id, value: session[:user_id] %><br>
<%= f.button :submit %>

<% end %>