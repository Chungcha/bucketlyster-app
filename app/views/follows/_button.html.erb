
<% if @list.follows.any? {|follow| follow.audience_id == session[:user_id]} %>
   <h5><%= button_to "UnFollow #{@list.title}",     
       follow_user_path, method: "POST" %></h5>
<% else %>
   <h5><%= button_to "Follow #{@list.title}", follow_user_path, 
       method: "POST"  %></h5>
<% end %>